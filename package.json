{
  "scripts": {
    "build": "node ./build.mjs",
    "gcc": "google-closure-compiler --js_output_file optimizer.out.js --js",
    "format-debug": "prettier . --debug-check",
    "format": "prettier . --write"
  },
  "type": "module",
  "dependencies": {
    "js-levenshtein": "^1.1.6",
    "mathjs": "^11.7.0",
    "prettier": "^3.2.5"
  },
  "devDependencies": {
    "@types/bun": "^1.1.3",
    "google-closure-compiler": "^20230228.0.0",
    "typescript": "^5.0.2"
  }
}
